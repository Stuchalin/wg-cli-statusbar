import Foundation

/// Единственный литерал синтетической канарейки секрета во всём Sources/Tests
/// (гейт Task 5 требует ровно одно вхождение — эту строку). Фикстуры тестов
/// конфига кладут её только в значения канонических назначений
/// PrivateKey/PresharedKey; поисковые проверки (ответы демона, бандл, логи
/// тестов/приложения/хелпера) доказывают контракт маскирования, не прикасаясь
/// к настоящим ключам. Не реальный ключ — просто меченая строка.
let configSecretCanary = "WGSTATUSBAR_SECRET_CANARY_7F2E9C41"

/// Вторая метка для PresharedKey: отличается от приватной, чтобы ассерты
/// ловили подмену одного поля другим; собирается из канарейки, литерала в
/// исходниках не добавляет.
let configPresharedCanary = configSecretCanary + "-PSK"
